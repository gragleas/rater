# rater
game rater

```
sudo apt-get update
sudo apt-get install git python3.8 python3-pip
cd
git clone https://github.com/gragleas/rater.git
cd ~/rater
pip install -r requirements.txt
python3 game_rater.py
```
